<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FWMobile.Modules.Races.RaceChoicePage"
             Title="Race Choice">
  <ListView ItemsSource="{Binding Races}" ItemTapped="RaceTapped" SelectedItem="{Binding SelectedRace}" >
    <ListView.ItemTemplate>
      <DataTemplate>
        <TextCell Text="{Binding City}" Detail="{Binding Title}"></TextCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>